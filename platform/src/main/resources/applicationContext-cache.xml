<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:hz="http://www.hazelcast.com/schema/spring"
       xmlns="http://www.springframework.org/schema/beans"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
              http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
              http://www.hazelcast.com/schema/spring
              http://www.hazelcast.com/schema/spring/hazelcast-spring-3.4.xsd">

    <hz:hazelcast id="hazelcastInstance">

        <hz:config>
            <hz:instance-name>bpmt-hazelcast-instance</hz:instance-name>
            <hz:group name="#{config['hazelcast.group.name']?:'bpmt'}"
                      password="#{config['hazelcast.group.password']?:'bpmt'}"/>
            <hz:management-center enabled="#{config['hazelcast.management.center.enable']?:'false'}" url="#{config['hazelcast.management.center.url']?:'http://localhost:8080/mancenter'}"/>
            <hz:properties>
                <hz:property name="hazelcast.merge.first.run.delay.seconds">5</hz:property>
                <hz:property name="hazelcast.merge.next.run.delay.seconds">5</hz:property>
                <hz:property name="hazelcast.prefer.ipv4.stack">true</hz:property>
            </hz:properties>
            <hz:network port="#{config['hazelcast.port']?:'5701'}" port-auto-increment="true">
                <hz:join>
                    <hz:multicast enabled="#{config['hazelcast.multicast']?:'false'}" multicast-group="#{config['hazelcast.multicast.group']?:'224.2.2.3'}" multicast-port="#{config['hazelcast.multicast.port']?:'54327'}"/>
                    <hz:tcp-ip enabled="#{config['hazelcast.tcpip']?:'false'}">
                        <hz:members>#{config['hazelcast.tcpip.members']?:'127.0.0.1'}</hz:members>
                    </hz:tcp-ip>
                </hz:join>
            </hz:network>
        </hz:config>

    </hz:hazelcast>

    <!-- 动态表缓存 -->
    <bean id="cacheManager" class="com.hazelcast.spring.cache.HazelcastCacheManager">
        <constructor-arg ref="hazelcastInstance"/>
    </bean>

</beans>